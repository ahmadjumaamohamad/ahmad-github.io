/* stating program v dynamic
- Dynamic programs use global v local variables for system resources.
*/
/*fullScreen();
int appWidth = displayWidth;
int appHeight = displayHeight;
String title = "1"; 
float rectHeight = appHeight*1/10;
rect(appWidth*1/4, appHeight*1/4, appWidth*1/2,appHeight*1/10);
//String[] fontList = PFont.list();
//println("Starting of console");
//printArray(fontList);
int fontSize = appHeight; // not font bigger than displayHeight, self terminating algorithm.
PFont titleFont = createFont ("Calibri Bold", fontSize);
//fontSize = 86;
//textSize (fontSize);
float fontAspectRatio_CalibriBold = 86.0 / float(appHeight*1/10);// recall casting
//indexes are usually integers
for ( float i=1; fontSize/rectHeight> fontAspectRatio_CalibriBold ; i++) {
}//End font Calculator.

textFont(titleFont, fontSize);
textAlign (CENTER, BOTTOM);// [LEFT , CENTER, RIGTH,]
fill(0);
text(title,appWidth*1/4, appHeight*1/4, appWidth*1/2,appHeight*1/10);
fill(155);*/
